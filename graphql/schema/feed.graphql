union FeedItem = VideoFeedItem

type VideoFeedItem {
    channel: Channel!
    video: Video!
}

type FeedEdge {
    cursor: Cursor!
    node: FeedItem!
}

type FeedConnection {
    pageInfo: PageInfo!
    totalCount: Int!
    edges: [FeedEdge!]!
    nodes: [FeedItem!]!
}