type Query {
    apiVersion: String!
    provider: Provider!
    viewer: Viewer!
    search(query: String!, take: Int!, after: Cursor): SearchResultConnection!
    feed(take: Int!, after: Cursor): FeedConnection!
}