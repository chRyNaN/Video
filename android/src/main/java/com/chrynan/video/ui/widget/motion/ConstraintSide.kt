package com.chrynan.video.ui.widget.motion

import androidx.constraintlayout.widget.ConstraintSet

enum class ConstraintSide(val value: Int) {

    START(value = ConstraintSet.START),
    END(value = ConstraintSet.END),
    TOP(value = ConstraintSet.TOP),
    BOTTOM(value = ConstraintSet.BOTTOM)
}